{
  "name": "vendor-json-server",
  "version": "1.0.0",
  "description": "JSON Server for vendor app",
  "scripts": {
    "start": "json-server --watch db.json --port 5000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
